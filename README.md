# iatecam-interview
Repo to iatecam interview

To run backend:
```
cd backend
docker compose up
docker compose run app alembic upgrade head
```

To run frontend
```
cd frontend
npm install
npm run start
```

http://localhost:4200/login
